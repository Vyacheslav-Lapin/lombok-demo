<?xml version="1.0" encoding="UTF-8"?>
<ruleset xmlns="https://www.mojohaus.org/VERSIONS/RULE/3.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
          https://www.mojohaus.org/VERSIONS/RULE/3.0.0
          https://www.mojohaus.org/versions/versions-model/xsd/rule-3.0.0.xsd">

  <rules>

    <rule groupId="org.springframework.cloud" artifactId="spring-cloud-*">
      <ignoreVersions>
        <ignoreVersion type="regex">1\.0\.\d\.\w+</ignoreVersion>
      </ignoreVersions>
    </rule>

    <rule groupId="org.projectlombok" artifactId="lombok">
      <ignoreVersions>
        <!-- ignoring all the versions lower than 1.18.46. List of versions: https://central.sonatype.com/artifact/org.projectlombok/lombok/versions -->
        <ignoreVersion type="regex">[01]\.1[864210]\.(\d|[123]\d|4[024])(-RC\d)?</ignoreVersion>
        <ignoreVersion>0.9.3</ignoreVersion>
      </ignoreVersions>
    </rule>

<!--    <rule groupId="org.checkerframework" artifactId="checker*">-->
<!--      <ignoreVersions>-->
<!--        &lt;!&ndash; ignoring all the versions 4.0.0 and 3.55.1. List of versions: https://central.sonatype.com/artifact/org.checkerframework/checker/versions &ndash;&gt;-->
<!--        <ignoreVersion>4.0.0</ignoreVersion>-->
<!--        <ignoreVersion>3.55.1</ignoreVersion>-->
<!--        <ignoreVersion>3.54.1</ignoreVersion>-->
<!--        <ignoreVersion>3.54.0</ignoreVersion>-->
<!--      </ignoreVersions>-->
<!--    </rule>-->

  </rules>

  <ignoreVersions>
    <!-- Ignore Alpha's, Beta's, release candidates and milestones -->
    <ignoreVersion type="regex">(?i).*Alpha(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*Beta(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*-B(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*RC(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*CR(?:-?\d+)?</ignoreVersion>
    <ignoreVersion type="regex">(?i).*M(?:-?\d+)?</ignoreVersion>

    <ignoreVersion type="regex">(?i).*(java[6,7]|jre[6,7])</ignoreVersion>
  </ignoreVersions>

</ruleset>
