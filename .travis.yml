language: java
jdk: openjdk19
# install: true
cache:
  directories:
    - /.m2
script:
  - mvn -N io.takari:maven:wrapper -Dmaven=3.8.7
  - ./mvnw verify
after_success:
  - bash <(curl -s https://codecov.io/bash)
